# Pneuma Agents

## Pneuma Agent

The **Pneuma Agent** adds Kubernetes namespaces to the platform. It collects the namespace name, Istio injection setting, and environment-specific Google service account emails, then updates all environment variable files and opens a pull request on `pt-pneuma`.
